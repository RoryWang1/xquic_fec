#include <stdio.h>
#include <xquic/xquic.h>

int main() {
    printf("Successfully linked with xquic!\n");
    // 只是简单调用一个 xquic 的函数或者宏来确保链接成功
    // xqc_engine_create 需要很多参数，这里先暂时只打印
    return 0;
}
